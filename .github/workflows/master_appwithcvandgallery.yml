name: build-and-deploy

on:
  push:
    branches:
      - master
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: windows-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.0.x'

    - name: Restore dependencies
      run: dotnet restore MyCvAppWithGalleryFeature.sln

    - name: Build
      run: dotnet build MyCvAppWithGalleryFeature.sln --no-restore

    - name: Publish
      run: dotnet publish MyCvAppWithGalleryFeature.sln --configuration Release --output ./publish_output

    - name: Deploy to Azure Web App
      uses: azure/webapps-deploy@v2
      with:
        app-name: 'AppwithCVandGallery'
        publish-profile: ${{ secrets.AZUREAPPSERVICE_PUBLISHPROFILE }}
        package: ./publish_output
